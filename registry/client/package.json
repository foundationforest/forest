{
  "name": "@forest/registry-client",
  "version": "0.0.0",
  "private": true,
  "description": "Builds a Forest registration: the code, the Merkle path, the proof, compressed points, and the transaction a fee payer co-signs.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "node --test test/client.test.ts",
    "test:validator": "node --test test/validator.test.ts",
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "fixtures": "node scripts/fixtures.ts"
  },
  "dependencies": {
    "@noble/hashes": "2.4.0",
    "@semaphore-protocol/group": "4.12.1",
    "@semaphore-protocol/identity": "4.12.1",
    "@solana/web3.js": "1.99.0",
    "poseidon-lite": "0.3.0",
    "snarkjs": "0.7.5"
  },
  "devDependencies": {
    "@solana/spl-token": "0.4.9",
    "@types/node": "22.20.2",
    "typescript": "5.9.3"
  }
}
