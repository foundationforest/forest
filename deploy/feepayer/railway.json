{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/feepayer/Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "healthcheckPath": "/liveness",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ALWAYS"
  }
}
