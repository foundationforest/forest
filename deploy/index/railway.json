{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/index/Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "healthcheckPath": "/robots.txt",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ALWAYS"
  }
}
