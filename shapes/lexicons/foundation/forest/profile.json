{
  "lexicon": 1,
  "id": "foundation.forest.profile",
  "defs": {
    "main": {
      "type": "record",
      "description": "A profile: the public card of one folder. One per folder, keyed 'self'.",
      "key": "literal:self",
      "record": {
        "type": "object",
        "required": ["name", "market", "role", "createdAt"],
        "properties": {
          "market": {
            "type": "string",
            "description": "The one market this profile lives in, as the markets directory spells it. A profile is one folder in one market; a badge counts for it only under market/role.",
            "maxLength": 64
          },
          "role": {
            "type": "string",
            "description": "Its side in that market: seller or buyer in a two-sided market, peer in a one-sided one.",
            "maxLength": 64
          },
          "name": {
            "type": "string",
            "description": "Display name.",
            "maxGraphemes": 64,
            "maxLength": 640
          },
          "photo": {
            "type": "blob",
            "description": "Profile photo.",
            "accept": ["image/png", "image/jpeg"],
            "maxSize": 1000000
          },
          "contact": {
            "type": "string",
            "description": "How to reach this profile, in the owner's words.",
            "maxGraphemes": 256,
            "maxLength": 2560
          },
          "about": {
            "type": "string",
            "description": "What I do.",
            "maxGraphemes": 1000,
            "maxLength": 10000
          },
          "wallet": {
            "type": "string",
            "description": "Declared wallet: the Solana public key this profile is paid at, base58.",
            "minLength": 32,
            "maxLength": 44
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
